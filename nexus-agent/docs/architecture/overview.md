# 架构设计

Universal Agent MVP 采用现代化的分层架构设计，将不同职责分离到独立的层中，提高代码的可维护性、可扩展性和可测试性。

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    🎯 接口层 (API Layer)                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  🌐 REST API (FastAPI)                  │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────┐
│                   🎯 核心层 (Core Layer)                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              🎯 UniversalAgent 接口                     │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────┐
│                  🎭 编排层 (Orchestration Layer)            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │ │
│  │  │  状态管理   │ │  工作流     │ │ 编排Agent   │       │ │
│  │  │ (State)     │ │ (Workflow)  │ │ (Agent)     │       │ │
│  │  └─────────────┘ └─────────────┘ └─────────────┘       │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────┐
│                 🔧 工具层 (Tools Layer)                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │ │
│  │  │  工具接口   │ │  注册器     │ │  执行器     │       │ │
│  │  │ (Base)      │ │ (Registry)  │ │ (Executor)  │       │ │
│  │  └─────────────┘ └─────────────┘ └─────────────┘       │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────┐
│                 🧠 LLM层 (LLM Layer)                        │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │ │
│  │  │  基础接口   │ │   工厂      │ │  提供商     │       │ │
│  │  │ (Base)      │ │  (Factory)  │ │ (Providers) │       │ │
│  │  └─────────────┘ └─────────────┘ └─────────────┘       │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────┐
│                 ⚙️ 配置层 (Config Layer)                    │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                 📋 配置管理 (Settings)                  │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 架构设计原则

### 1. 单一职责原则 (SRP)
每个层级只负责一个明确的功能：
- **LLM层**: 专门处理LLM提供商集成
- **工具层**: 专注工具管理
- **编排层**: 负责对话流程控制
- **核心层**: 提供统一接口
- **接口层**: 处理外部请求

### 2. 依赖倒置原则 (DIP)
高层模块不依赖低层模块，通过接口进行交互：
- 编排层依赖抽象的工具接口
- 核心层依赖抽象的编排接口
- 接口层依赖抽象的核心接口

### 3. 开闭原则 (OCP)
对扩展开放，对修改封闭：
- 新增LLM提供商无需修改现有代码
- 添加新工具不需要更改核心逻辑
- 扩展API接口不影响业务逻辑

### 4. 接口隔离原则 (ISP)
客户端不依赖不需要的接口：
- 每个层级只暴露必要的接口
- 依赖最小化原则
- 避免接口污染

## 数据流

### 请求处理流程

```
用户请求
    ↓
🌐 接口层 (FastAPI)
    ↓ (路由解析)
🎯 核心层 (UniversalAgent)
    ↓ (业务编排)
🎭 编排层 (OrchestratorAgent)
    ↓ (状态管理)
🧠 LLM层 (LLM Provider)
    ↙ (工具调用)
🔧 工具层 (Tool Executor)
    ↓ (结果返回)
🎭 编排层 (状态更新)
    ↓
🎯 核心层 (响应格式化)
    ↓
🌐 接口层 (HTTP响应)
    ↓
用户响应
```

### 工具调用流程

```
对话进行中...
    ↓
🎭 编排层检测工具调用需求
    ↓
🔧 工具层查找工具
    ↓
🔧 工具层执行工具
    ↓
🔧 工具层返回结果
    ↓
🎭 编排层更新状态
    ↓
🧠 LLM层生成最终响应
```

## 层级详细说明

### 🧠 LLM层 (LLM Layer)

**职责范围**:
- 统一LLM提供商接口
- 管理不同的AI服务集成
- 处理API认证和错误重试
- 提供统一的调用接口

**组件**:
- `LLMProvider`: 抽象基类，定义标准接口
- `LLMFactory`: 工厂模式，创建具体提供商实例
- `OpenAIProvider`: OpenAI服务集成
- `AnthropicProvider`: Anthropic服务集成
- `SiliconFlowProvider`: SiliconFlow服务集成

**设计模式**:
- **工厂模式**: 根据配置创建对应的LLM实例
- **策略模式**: 不同的提供商实现不同的调用策略
- **适配器模式**: 统一不同API的接口差异

### 🔧 工具层 (Tools Layer)

**职责范围**:
- 工具注册和发现
- 工具实例管理和生命周期
- 工具执行和结果处理
- 工具权限和安全控制

**组件**:
- `Tool`: 工具抽象基类
- `ToolRegistry`: 工具注册中心
- `ToolExecutor`: 工具执行器
- `CalculatorTool`: 内置计算器工具

**设计模式**:
- **注册表模式**: 集中管理工具实例
- **命令模式**: 封装工具执行逻辑
- **插件模式**: 支持动态加载工具

### 🎭 编排层 (Orchestration Layer)

**职责范围**:
- 对话状态管理
- 工作流编排和执行
- 工具调用协调
- 错误处理和重试

**组件**:
- `AgentState`: LangGraph状态定义
- `WorkflowManager`: 基于LangGraph的工作流管理器
- `OrchestratorAgent`: 编排Agent核心逻辑

**设计模式**:
- **状态机模式**: 使用LangGraph管理对话状态
- **中介者模式**: 协调各组件之间的交互
- **责任链模式**: 处理复杂的对话流程

### 🎯 核心层 (Core Layer)

**职责范围**:
- 提供统一的外部接口
- 协调各层级组件
- 处理跨层级的业务逻辑
- 保证API稳定性

**组件**:
- `UniversalAgent`: 统一接口类

**设计模式**:
- **外观模式**: 简化复杂子系统的接口
- **适配器模式**: 适配不同层级的接口
- **桥接模式**: 解耦抽象和实现

### 🌐 接口层 (API Layer)

**职责范围**:
- HTTP请求处理和路由
- 请求验证和序列化
- 响应格式化和错误处理
- API文档自动生成

**组件**:
- FastAPI应用
- 请求/响应模型
- 中间件和异常处理器

**设计模式**:
- **控制器模式**: 处理HTTP请求分发
- **管道模式**: 请求处理管道
- **装饰器模式**: 路由和中间件装饰

### ⚙️ 配置层 (Config Layer)

**职责范围**:
- 配置文件的加载和解析
- 配置验证和类型检查
- 环境变量管理
- 配置热重载支持

**组件**:
- `Settings`: 配置管理类
- YAML/JSON配置解析器
- 环境变量处理器

## 通信机制

### 同步通信
- REST API调用
- 直接方法调用
- 管道模式数据流

### 异步通信
- asyncio协程
- 异步HTTP客户端
- 事件驱动架构

## 扩展点

### LLM提供商扩展
```python
from llm.base import LLMProvider

class CustomProvider(LLMProvider):
    def create_llm(self):
        # 自定义LLM实现
        return CustomLLM(api_key=self.api_key)

# 注册新提供商
from llm.factory import LLMFactory
LLMFactory.register_provider("custom", CustomProvider)
```

### 工具扩展
```python
from tools.base import Tool

class CustomTool(Tool):
    def __init__(self):
        super().__init__("custom", "自定义工具")

    async def execute(self, **kwargs):
        # 自定义工具逻辑
        return "自定义结果"

# 注册工具
from tools.registry import ToolRegistry
registry = ToolRegistry()
registry.register(CustomTool())
```

### API扩展
```python
from fastapi import APIRouter
from core import UniversalAgent

def create_custom_router(agent: UniversalAgent) -> APIRouter:
    router = APIRouter()

    @router.post("/custom-endpoint")
    async def custom_endpoint(request: CustomRequest):
        # 自定义API逻辑
        return await agent.process_custom_request(request)

    return router
```

## 性能优化

### 缓存策略
- LLM响应缓存
- 工具执行结果缓存
- 配置信息缓存

### 连接池
- HTTP客户端连接复用
- 数据库连接池
- Redis连接池

### 异步处理
- 非阻塞I/O操作
- 并发工具执行
- 流式响应处理

## 监控和可观测性

### 指标收集
- 请求响应时间
- 错误率统计
- 资源使用情况

### 日志记录
- 结构化日志
- 请求追踪
- 错误堆栈跟踪

### 健康检查
- 组件状态监控
- 依赖服务检查
- 性能指标报告

## 安全考虑

### API安全
- 请求频率限制
- API密钥认证
- 输入验证和清理

### 数据安全
- 敏感信息加密
- 传输层安全(TLS)
- 审计日志记录

### 工具安全
- 工具执行权限控制
- 沙箱环境执行
- 资源使用限制

## 部署架构

### 单机部署
```
┌─────────────────────────────────────┐
│           Universal Agent           │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ LLM │ │Tool │ │Orch.│ │ API │   │
│  │Layer│ │Layer│ │Layer│ │Layer│   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
└─────────────────────────────────────┘
```

### 分布式部署
```
┌─────────────┐    ┌─────────────┐
│   API层     │    │   监控      │
│  (Load      │    │  (Metrics)  │
│   Balancer) │    │             │
└─────────────┘    └─────────────┘
        │                  │
┌─────────────┐    ┌─────────────┐
│   核心层    │────│   缓存      │
│ (Business   │    │  (Redis)    │
│   Logic)    │    │             │
└─────────────┘    └─────────────┘
        │
┌─────────────┐    ┌─────────────┐
│   编排层    │    │   状态      │
│ (Workflow)  │────│  (SQLite/   │
│             │    │   Redis)    │
└─────────────┘    └─────────────┘
        │
┌─────────────┐    ┌─────────────┐
│   工具层    │    │   LLM       │
│ (Tools)     │────│  (External  │
│             │    │   APIs)     │
└─────────────┘    └─────────────┘
```

这个分层架构设计为Universal Agent提供了坚实的技术基础，支持未来的扩展和演进。
